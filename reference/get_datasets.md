# Get datasets

Get datasets

## Usage

``` r
get_datasets(connection = NULL)
```

## Arguments

- connection:

  A connection to the peekbank database

## Value

A tibble of Datasets data.

## Examples

``` r
if (FALSE) { # \dontrun{
get_datasets()
} # }
```
